
{
    console.log("funcionando");

    /* TRES ESTRUCTURAS */

    // el CICLO FOR

    // el CICLO WHILE

    // el CICLO DO WHILE

    /*
        ITERATIVO = REPETITIVO
    */


    console.log("esto se muestra antes de comenzar el ciclo");

    for(let iterador = 0;iterador <= 10;iterador = iterador + 1)
    {
        // primer parametro es la variable que me sirve de iterador
        // segundo parametro es la condición de permanencia dentro del ciclo for
        // tercer parametro es como ira creciendo el iterador

        console.log(iterador);

        console.log("esta sentencia se debería repetir varias veces");

    }

    console.log("esto se muestra cuando el ciclo for termino");

    for(let iterador = 0;iterador <= 10;iterador = iterador + 2)
        {
            // primer parametro es la variable que me sirve de iterador
            // segundo parametro es la condición de permanencia dentro del ciclo for
            // tercer parametro es como ira creciendo el iterador
    
            console.log(iterador);
    
            console.log("esta sentencia se debería repetir varias veces");
    
        }

}